version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: mptoo
      POSTGRES_PASSWORD: password
      POSTGRES_DB: mptoo
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U mptoo"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
