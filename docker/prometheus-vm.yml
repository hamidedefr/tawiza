# Prometheus configuration for VM Sandbox monitoring
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'vm-sandbox'
    static_configs:
      - targets: ['vm-sandbox-manager:8085']
    metrics_path: /metrics

  - job_name: 'node-exporter'
    static_configs:
      - targets: ['vm-sandbox-manager:9100']

  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
